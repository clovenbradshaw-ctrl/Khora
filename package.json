{
  "name": "eo-substrate",
  "version": "0.1.0",
  "description": "EO Substrate: a kernel + Matrix-backed log + generated surfaces for building EO-addressed applications.",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "smoke:matrix": "node scripts/smoke-test-matrix.mjs",
    "load:catalog": "node scripts/load-data-source-catalog.mjs"
  },
  "devDependencies": {
    "vitest": "^2.1.9"
  },
  "dependencies": {
    "jszip": "^3.10.1",
    "matrix-js-sdk": "^41.9.0-rc.0"
  }
}
